#!/bin/sh

NODEJS_LANGUAGE_DIR="$(realpath $(dirname $0))"
PRYBAR_ROOT_DIR="$(dirname $(dirname "${NODEJS_LANGUAGE_DIR}"))"
NODEJS_ASSET_DIR="${PRYBAR_ROOT_DIR}/prybar_assets/nodejs"

yarn install --cwd "${NODEJS_ASSET_DIR}" --production
